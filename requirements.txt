# Floors chosen for numpy 2.x compatibility (the active env uses numpy 2.x);
# numpy upper-bounded to avoid a future 3.0 ABI break across the stack.
numpy>=2.0,<3
scipy>=1.13          # first release with numpy 2 support; imported by segment_ct.py + strut metrics
pandas>=2.2.2        # numpy 2 support; per_strut_metrics.csv handling
scikit-image>=0.24   # numpy 2 support
tifffile>=2024.7.2
trimesh>=4.4
pyvista>=0.44        # numpy 2 support; render_lattice_3d offscreen
matplotlib>=3.9      # numpy 2 support
fastmcp>=2.0
napari[pyqt6]>=0.5
